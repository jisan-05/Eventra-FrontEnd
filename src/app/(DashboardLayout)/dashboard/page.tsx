/**
 * Default slot for parallel routes under /dashboard (required by Next.js App Router).
 * Actual dashboard UI is rendered from @admin or @user parallel slots.
 */
export default function DashboardSlotPage() {
  return null;
}
